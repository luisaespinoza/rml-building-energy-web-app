# ONNX Runtime Web files

Run this command after installing dependencies:

```bash
npm run copy:ort
```

The script copies `.wasm` and helper files from `onnxruntime-web` into this folder so the static app can run local browser inference after deployment.
